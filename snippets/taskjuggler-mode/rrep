# name: resourcereport
# key: rrep
# license: GPL-3.0-or-later
# --
resourcereport ${1:id} "${2:Report Title}" {
  formats ${3:html}
  columns name, effort, rate, weekly
  loadunit shortauto
  hideresource ~isleaf()
  hidetask @all
  $0
}
